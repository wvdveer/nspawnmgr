package com.nspawnmgr.guacamole;

/** How a GuacamolePropertyField should be rendered/submitted on /admin/settings. */
public enum GuacamolePropertyFieldType {
    TEXT, PASSWORD, NUMBER, CHECKBOX, SELECT
}
