package com.nspawnmgr.domain;

/** Desktop environment to install alongside RDP/VNC - NONE matches pre-existing RDP-only behavior. */
public enum DesktopManager {
    NONE, GNOME, KDE_STANDARD, XFCE4
}
